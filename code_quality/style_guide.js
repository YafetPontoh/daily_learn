/** Contoh dari setiap dev berbeda2 gaya penulisannya */
// let no = 'Semicolon';

// const my_fav_color = '#1122C4';

// let myNumber = 3;
// console.log(myNumber);

// var random = 1;  // Declaring random to one

// if(foo) foo++;

// let a=b=c=5;

// Metode Manual -> Sesuai dengan dokumentasi

const no = 'Semicolon';
console.log(no);

/** Ini diperbaiki dalam penulisan variabel */ 
const myFavColor = '#112432';
console.log(myFavColor);

/** Mengutamakan Const */
const myNumber = 3;
console.log(myNumber);

/** Memperbaiki comments yang sejajar dengan code
 * Utamakan const
 */
// Declaring Random to One
const random = 1;
console.log(random);

/** Penggunaan Curly Bracket 
 * mendeklarasikan var foo
*/
const foo = true;
let test = 1;
if(foo){
    return test += 1;
}
console.log(foo);

/** Mengutamakan Const dan memperbaiki assignment yang berlipat */
const a = 5;
const b = 5;
const c = 5;
console.log((a+b+c)/3);
